# RED 基线:不加载 skill 的裸代理输出(2026-09-07)

四个样例输入(去掉测试说明头)交给只读输入文件、不接触 skill 的代理,指令为一句普通请求("改写成可投 SCI 的英文 Methods 段"等)。输出 `out-*.md`,用与 skill 测试相同的 `samples/expected-points-*.md` 打分,结果见 `../baseline-delta-2026-09-07.md`。
