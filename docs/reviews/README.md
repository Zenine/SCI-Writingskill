# 审阅报告

按批次对 `skills/sci-writing/` 做只读审阅,报告命名 `YYYY-MM-DD-review-R<n>.md`。审阅代理不改 skill 文件,只写报告;修改由主会话汇总后另行执行。

每条发现固定格式:`[级别] 文件:行号 — 问题 — 建议改法`。级别:blocker(规则矛盾/事实错误/疑似抄录)、major(不可执行/缺关键规则/英文模板有错)、minor(措辞/格式/冗余)。
